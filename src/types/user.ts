export type UserRequest = {
    userName: string;
    zanId: string
    email: string
    password: string
}

export type UserResponse = {
    id: string
    userName: string
    zanId: string
    email: string
    password: string
}